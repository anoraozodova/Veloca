spring:
  datasource:
    url: jdbc:postgresql://dpg-d72fcendiees739nskhg-a:5432/veloce_h2y0
    username: veloce_user
    password: LeKAowr2rVaLhqN3MmgSCthRF1V2ICJ7
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: ${PORT:8080}